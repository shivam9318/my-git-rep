name: Hello World
'on': push
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo text
        run: | 
          echo "Hello, world!"
          sudo apt install nginx
          nginx -s reload
          curl -Il http://localhost:80
